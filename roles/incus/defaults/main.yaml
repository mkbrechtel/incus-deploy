---
incus_release: "{{ incus_release | default('stable') }}"
incus_roles: "{{ incus_roles | default([]) }}"
incus_init: "{{ incus_init | default('{}') }}"
incus_ip_address: "{{ ansible_default_ipv4['address']) }}"
incus_name: "{{ incus_name | default('') }}"
